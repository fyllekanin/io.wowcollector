import type { Sort } from '@/types/filters';

export const RENDER_TYPES = {
  0: 'grid-compact',
  1: 'grid',
  2: 'list',
};

export const SORT_TYPES: Sort[] = [
  'Collected',
  'Not Collected',
  'Name Ascending',
  'Name Descending',
];
